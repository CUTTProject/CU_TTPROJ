package com.university.timetable_scheduler.service;

import java.util.UUID;

public interface UtilityService {
    static String generateConflictKey(UUID id1, UUID id2) {
        return "";
    }
    static String validate(UUID id1, UUID id2) {
        return "";
    }
}
