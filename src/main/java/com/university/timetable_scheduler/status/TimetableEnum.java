package com.university.timetable_scheduler.status;

public class TimetableEnum {
    public enum TimetableConflictReason {
        SAME_LECTURER,
        OVERLAPPING_STUDENTS,
        UNIVERSITY_HARD_RULES
    }
}
